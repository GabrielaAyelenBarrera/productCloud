<h1>Lista de usuarios</h1>
<ul>
    {{#if users.length}}
    {{#each users}}
        <li>{{this.first_name}} - {{this.email}}</li>
    {{/each}}
    {{else}}
    <li>No hay usuarios disponibles</li>
    {{/if}}
</ul>
